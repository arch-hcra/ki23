apiVersion: v1
kind: Service
metadata:
  name: ki23-service
spec:
  selector:
    app: ki23
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30280
  type: NodePort
